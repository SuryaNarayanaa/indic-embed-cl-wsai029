# Documentation

Reports, exported PDFs, certificates, and supporting writeups live under `reports/`.

The main repository README should describe the experiments. Detailed writeups and official documents can remain here so the repo root stays focused on code and notebooks.
